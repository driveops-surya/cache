{
  "name": "github-actions-caching-demo",
  "version": "1.0.0",
  "description": "Demo project for GitHub Actions caching concepts",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Running tests...' && sleep 2 && echo 'Tests passed!'",
    "build": "echo 'Building project...' && mkdir -p dist && echo 'console.log(\"Hello from build!\");' > dist/index.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}